import type { RetailerInput } from './schemas';

// A new shop = one entry here + its links on products.
export const retailers: RetailerInput[] = [
  { id: 'emag', name: 'eMAG' },
  { id: 'amazon', name: 'Amazon' },
];
